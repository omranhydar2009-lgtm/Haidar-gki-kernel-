name: Build and Package Psych Engine

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build-linux:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Setup Haxe
      uses: krdlab/setup-haxe@v1
      with:
        haxe-version: 4.3.4

    - name: Install Ubuntu Dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y libgl1-mesa-dev libglu1-mesa-dev libalut-dev libasound2-dev libx11-dev libxext-dev libxi-dev libxrandr-dev libxinerama-dev libxcursor-dev libvlc-dev libvlccore-dev

    - name: Install Haxelibs
      run: |
        haxelib setup ~/haxelib
        haxelib install lime
        haxelib install openfl
        haxelib install flixel
        haxelib run lime setup

    # خطوة اختيارية لمعاينة المجلدات إذا فشل البناء مجدداً
    - name: List Files
      run: |
        echo "=== Current Directory Content ==="
        ls -la
        echo "=== Searching for Project.xml ==="
        find . -name "Project.xml" -o -name "project.xml"

    - name: Compile Game
      run: |
        # البحث عن المجلد الذي يحتوي على ملف المشروع والدخول إليه تلقائياً
        PROJECT_DIR=$(dirname $(find . -name "Project.xml" -o -name "project.xml" | head -n 1))
        echo "Found project in: $PROJECT_DIR"
        cd "$PROJECT_DIR"
        haxelib run lime build linux -release

    - name: Zip Psych Engine Release
      run: |
        # البحث عن مجلد البناء الناتج وضغطه
        # افتراضياً Lime تخرج الناتج في export/release/linux/bin أو داخل مجلد المشروع
        PROJECT_DIR=$(dirname $(find . -name "Project.xml" -o -name "project.xml" | head -n 1))
        
        if [ -d "$PROJECT_DIR/export/release/linux/bin" ]; then
          cd "$PROJECT_DIR/export/release/linux/bin"
        elif [ -d "game_folder/PsychEngine" ]; then
          cd game_folder/PsychEngine
        else
          # إذا لم يجد المسارات السابقة، سيقوم بضغط مجلد المشروع بالكامل كحل احتياطي
          cd "$PROJECT_DIR"
        fi
        
        zip -r "$GITHUB_WORKSPACE/psych-engine-release.zip" ./*

    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: PsychEngine-Linux-Build
        path: psych-engine-release.zip
