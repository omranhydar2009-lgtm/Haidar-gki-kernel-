name: Build ReSukiSU Kernel LI9 (GKI 6.6)
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Free Disk Space
        uses: jlumbroso/free-disk-space@v1.3.1

      - name: Install Build Tools
        run: |
          sudo apt update
          sudo apt install -y git-core gnupg flex bison build-essential zip curl zlib1g-dev libncurses5-dev x11proto-core-dev libx11-dev libgl1-mesa-dev libxml2-utils xsltproc unzip python3 bc

      - name: Sync Kernel Source
        run: |
          # Syncing Android 15 Kernel 6.6 Source (Specific Tag)
          git clone --depth 1 -b android15-6.6-2025-02_r1 https://android.googlesource.com/kernel/common common
          
      - name: Integrate ReSukiSU
        run: |
          cd common
          # Install ReSukiSU using official setup script
          curl -LSs "https://raw.githubusercontent.com/ReSukiSU/ReSukiSU/main/kernel/setup.sh" | bash

      - name: Apply Manual Hooks
        run: |
          cd common
          # Inject ReSukiSU Hooks into fs/read_write.c
          sed -i '/vfs_read(struct file/i #ifdef CONFIG_KSU\nextern int ksu_handle_vfs_read(struct file *file, char __user *buf, size_t count, loff_t *pos);\n#endif' fs/read_write.c

      - name: Configure and Build
        run: |
          cd common
          # Find gki_defconfig and append ReSukiSU configs
          CONF_PATH=$(find arch/arm64/configs/ -name "gki_defconfig" | head -n 1)
          echo "[+] Found config at: $CONF_PATH"
          
          echo "CONFIG_KSU=y" >> $CONF_PATH
          echo "CONFIG_KSU_MANUAL_HOOK=y" >> $CONF_PATH
          
          # Use the generic gki_defconfig for building
          make ARCH=arm64 O=out gki_defconfig
          
          # Start building with LLVM/Clang
          make ARCH=arm64 O=out CROSS_COMPILE=aarch64-linux-android- LLVM=1 -j$(nproc)

      - name: Package with AnyKernel3
        run: |
          git clone https://github.com/osm0sis/AnyKernel3
          # Copy the compiled kernel image
          cp common/out/arch/arm64/boot/Image AnyKernel3/
          cd AnyKernel3
          # Setup AnyKernel3 for Tecno LI9
          sed -i 's/device.name1=.*/device.name1=LI9/g' anykernel.sh
          sed -i 's/block=.*/block=\/dev\/block\/by-name\/boot;/g' anykernel.sh
          zip -r ../ReSukiSU_Kernel_LI9.zip *

      - name: Upload Kernel
        uses: actions/upload-artifact@v4
        with:
          name: ReSukiSU-Kernel-LI9
          path: ReSukiSU_Kernel_LI9.zip
